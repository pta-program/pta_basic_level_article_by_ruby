# 1017 A除以B

- **分值：** 20分

### 题目描述

本题要求计算 $A/B$，其中 $A$ 是不超过 1000 位的正整数，$B$ 是 1 位正整数。你需要输出商数 $Q$ 和余数 $R$，使得 $A = B \times Q + R$ 成立。

### 输入格式：

输入在一行中依次给出 $A$ 和 $B$，中间以 1 个空格分隔。

### 输出格式：

在一行中依次输出 $Q$ 和 $R$，中间以 1 个空格分隔。

### 输入样例：
```
123456789050987654321 7
```

### 输出样例：
```
17636684150141093474 3
```


### 解题思路

本题的核心是：逐位进行十进制长除法，同时记录每一位余数。程序先读取题目输入，再按照上述规则完成数据处理，最后严格按照题目要求输出结果。实现时应优先根据题目约束选择合适的数据类型和数据结构，避免溢出或不必要的重复计算。

时间复杂度为 O(L)；空间复杂度取决于输入规模，主要用于保存题目数据和中间结果。

### 代码流程说明

1. 以字符串读入大整数 A，读入除数 B。
2. 从高位模拟除法，拼出商，记录最终余数。
3. 输出商与余数。

### 代码实现

```ruby
# 题目：1017-A除以B
# 实现原理：
# 本题的核心是：逐位进行十进制长除法，同时记录每一位余数
# 时间复杂度为 O(L)；空间复杂度取决于输入规模，主要用于保存题目数据和中间结果。
# 代码步骤：
# 1. 读取输入并初始化必要的数据结构。
# 2. 按题意完成核心计算，处理边界情况。
# 3. 按题目要求格式化并输出结果。
#
number, divisor = STDIN.read.split
divisor = divisor.to_i
remainder = 0
quotient = number.chars.map do |digit|
  current = remainder * 10 + digit.to_i
  remainder = current % divisor
  current / divisor
end.join.sub(/^0+/, '')
quotient = "0" if quotient.empty?
puts "#{quotient} #{remainder}"
```

### 代码流程图

```mermaid
flowchart TD
  A["开始"] --> B["读入 A 字符串与 B"]
  B --> C["从高位做长除"]
  C --> D["拼接商的每一位"]
  D --> E["得到余数 rem"]
  E --> F["输出商与 rem"]
  F --> G["结束"]
```

### 解题流程图

```mermaid
flowchart TD
  A["输入大整数 A 与除数 B"] --> B["逐位长除"]
  B --> C["得到商与余数"]
  C --> D["输出"]
  D --> E["结束"]
```

### 常见易错点

- A 可能很长，不能直接转普通整数时应模拟除法。
- 商的前导 0 要去掉；商为 0 时输出 0。
- 余数单独输出。
